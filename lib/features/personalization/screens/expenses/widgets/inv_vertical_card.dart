import 'package:flutter/material.dart';

class CInvHorizontalCard extends StatelessWidget {
  const CInvHorizontalCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
